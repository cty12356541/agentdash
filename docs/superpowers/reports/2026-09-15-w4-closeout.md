# agentdash W4 收口验证报告(评估清偿)

- 日期:2026-09-15
- 范围:W4 计划 7 任务(spec 基线 `2026-09-15-agentdash-w4-delta.md`)
- 结论:**全部完成,可发布 0.3.0**

## 验证真值

| 项 | 值 |
|---|---|
| 测试 | **188 通过 / 0 失败**(基线 177 + 行为/投影/空态 5 + proptest 6) |
| 三件套 | fmt 净 / clippy `--all-targets -- -D warnings` 零告警 / 全测绿 |
| 版本 | 0.3.0 三处(Cargo.toml / kits plugin.json / marketplace.json),`agentdash --version` 自检过 |
| dogfood | 台账 7/7 done;真实 gate 事件(cargo-test 188 passed、cargo-clippy 0 warnings)经 `agentdash hook` CLI 端到端入账并上板 |

## 行为变更改判清单(D1 承诺兑现)

既有 passed 判定**零翻转**(全部 passed 断言的 fixture 均显式带 `status: 0`)。实际改判三处,均为"无退出码证据"路径:

1. `three_hooks_replay`:Edit 工具行 exit `0 → null`(response 无证据,溯源字段不臆造);
2. `exit_code_variants`:尾三例(缺字段/非对象/Null)`0 → null`;
3. `summary_line_variants`:对象 fixture 补 `status: 0`(摘要测试意图不变),非对象例期望 `(exit unknown)`;折叠新语义由专测 `unknown_exit_gate_folds_failed_with_tailnote` 钉住。

## 帧证据

- `w4-close-panel.txt`:7/7 done + 双 gate passed + 三条批间屏障行(⇕)上板;
- `w4-close-graph.txt`:W4 台账 DAG(屏障边 02→03/04、04→05/06、05,06→07)。

## W4-006 首跑结果(tag 推送后补记)

- Release workflow(v0.3.0 触发)**四 target 全绿**,资产四件自动挂载
  (linux-x64 / macOS 双架构 / windows-msvc);CI(main)同批通过。
- 抽验 `agentdash-v0.3.0-aarch64-apple-darwin.tar.gz`:下载 → 解压 → 运行
  `--version` 输出 `agentdash 0.3.0`,二进制功能完好。
- **首跑发现一处打包瑕疵**:压缩包内二进制带 `agentdash-<tag>-<triple>`
  全名,与安装指引"解压即放入 PATH"不符。处置:① Release 说明如实标注
  本批资产需改名;② `release.yml` 修正为压缩包内恒名 `agentdash[.exe]`
  (下一 tag 生效)。重打 v0.3.0 属删除已发布 tag/release 的破坏性动作,
  留维护者裁定,报告不擅自执行。
- 遗留注记(非阻塞):Actions 提示 checkout/upload-artifact/gh-release 三个
  action 的 Node 20 弃用警告,后续升 major 版本时一并处理。
