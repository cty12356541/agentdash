# agentdash W5 收口验证报告(信任锚落地)

- 日期:2026-09-15
- 范围:W5 计划 4 任务(spec 基线 `2026-09-15-agentdash-w5-delta.md`)
- 结论:**全部完成,可发布 0.4.0**

## 验证真值

| 项 | 值 |
|---|---|
| 测试 | **195 通过 / 0 失败**(基线 189 + 契约/锚/投影/面板/详情/写回 7 - 断言并组) |
| 三件套 | fmt 净 / clippy `--all-targets -- -D warnings` 零告警 / 全测绿 |
| 版本 | 0.4.0 三处 + Cargo.lock,`--version` 自检过 |
| dogfood | 物证环**双向实证**(见下);台账 4/4 done;真实 gate 上板 |

## 主 feature:done_at 物证交叉核对(双向实证)

1. **亮起**:为 01/02/03 打真实 `done_at`(20:25,晚于此前最后通过门 14:06)→
   面板三行 `✓ … ?` 如实亮起(帧 `w5-close-panel-suspect.txt`);
2. **回落**:真跑三件套(195/0)+ hook CLI 登记真实通过门 → 对账锚刷新至 20:2x
   → `?` 全部回落(帧 `w5-close-panel.txt`)。
3. **边界如实**:`date +%z` 产出的 `+0800`(无冒号)不被解析 → 不打标记——
   "解析不了不怀疑、不虚报"按设计生效;`±HHMM` 基本格式容忍列为候选(W6)。

## 语义立场(spec D1,重申)

`?` = "自报无物证",非作弊指控:人工维护无戳不打;事件窗不在场不打;
时刻不可解析不打。写回 `d` 键自动盖章,`done→blocked` 摘自家戳。

## 帧证据

- `w5-close-panel-suspect.txt`:`?` 亮起态(3 处);
- `w5-close-panel.txt`:通过门刷新后回落态(4/4 done,双 gate passed);
- `w5-close-graph.txt`:W5 台账 DAG(单屏障 01,02,03 → 04)。

## 快速清偿

- W5-002:README / setup SKILL MSRV ≥1.85 → ≥1.88(let-chains 缘由注明);
- W5-003:`clock_slice` tui/panel 双份并入 render::mod,零行为变更。
