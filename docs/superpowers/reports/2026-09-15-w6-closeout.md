# agentdash W6 收口验证报告(输出可达性收口)

- 日期:2026-09-15
- 范围:W6 计划 5 任务(spec 基线 `2026-09-15-agentdash-w6-delta.md`)
- 结论:**全部完成,可发布 0.5.0**

## 验证真值

| 项 | 值 |
|---|---|
| 测试 | **199 通过 / 0 失败**(基线 195 + ±HHMM 6 例断言并入 merge 组 + svg CLI 3) |
| 三件套 | fmt 净 / clippy `-D warnings` 零告警 / 全测绿 |
| 版本 | 0.5.0 三处 + Cargo.lock,`--version` 自检过 |
| dogfood | svg 样张对仓内台账直出(`w6-close-graph.svg`,合法 XML);真实 gate 上板 |

## 交付明细

1. **±HHMM 容忍**(W6-001):`rfc3339_to_secs` 第三形态,单臂守卫统一两种
   偏移写法;6 例点断言(同折算/负偏移/长度≠5/6 字节歧义/时越界/分越界)。
   既有 `Z`/`±HH:MM` 语义逐字节不变。
2. **svg 输出**(W6-002):`render graph --format svg`(及 `--format=X` 同形),
   与 ansi 同一几何源不同渲染;`svg_edges`/`svg_boxes` 助手拆分;CLI 集成
   测试用 `CARGO_BIN_EXE`(合法 XML、两形态同义、缺省 ansi 逐字节不变、
   错误路径退 2)。
3. **proptest 回归落盘**(W6-004):`FileFailurePersistence::Direct` 指向
   crate 根 `proptest-regressions/`,失败案例可提交回放(SourceParallel 在
   纯二进制 crate 找不到 lib.rs 的 W4 注记就此销账)。
4. **install.ps1 静态审查**(W6-003,**零改码**):五项逐条核对——
   - `ConvertTo-Json -Depth 10`(嵌套钩子不被截断)✓
   - settings/gitignore 均无 BOM UTF-8(BOM 毒化解析器)✓
   - 损坏 JSON:备份 `.bak-agentdash` 后重建 ✓
   - 幂等:自家注册判定(`agentdash hook`/`record_event.py`)清除后重加,
     无关块原样保留 ✓
   - PreToolUse matcher `Task|Agent` 与 hooks.json 清单一致 ✓
   审查结论:与 install.sh 语义对齐,无缺陷。

## 过程语义发现(如实)

同秒对账:对账锚与 `done_at` 落在同一秒时,严格大于判定**不怀疑**——
同秒内先后不可分辨,存疑不足以指控,保守不标(负担落在怀疑方)。
已由点断言与实测双重确认(收口帧 `?` 计数 0,即全员物证在册)。

## 帧证据

- `w6-close-panel.txt`:4+1/5 done,双 gate passed,单屏障 `01,02,03,04 → 05`;
- `w6-close-graph.svg`:`render graph --format svg` 直出样张(合法 XML)。
