# agentdash W8 收口验证报告(zcode-kit)

- 日期:2026-09-15
- 范围:W8 计划 3 任务(spec 基线 `2026-09-15-agentdash-w8-delta.md`)
- 结论:**全部完成,可发布 0.7.0**(zcode 钩子待会话重开实测,如实标注)

## 验证真值

| 项 | 值 |
|---|---|
| 测试 | **205 通过 / 0 失败**(svg 断言改为波次无关后与基线一致) |
| 三件套 | fmt 净 / clippy `-D warnings` 零告警 / 全测绿 |
| 版本 | 0.7.0 三处 + marketplace,`--version` 自检过 |
| 实装 | **本仓即 ZCode 工作台**:`.zcode/config.json` 注册三钩子(enabled:true)、AGENTS.md 创建、gitignore 未被污染 |

## 交付

1. **kits/zcode**:hooks 模板(PostToolUse 全工具 / PreToolUse Task\|Agent /
   Stop,均 `--host zcode`)+ 安装器(**D1 只动 config.json 的 hooks 键**,
   enabled 置位,损坏备份重建,无 jq 打印手工指引)+ README。
2. **README 宿主矩阵**加 ZCode 行(公司内部);marketplace/plugin 描述加
   ZCode;一致性三支柱与 W7 同构。

## dogfood 实录(本仓实装,皆为真发现)

1. **安装器 gitignore 白名单冲突**:本仓以 `!/.agentdash/ledger.json` 例外
   提交台账,安装器无条件追加裸 `.agentdash/` 行会落在例外之后语义反转——
   zcode 安装器守卫改为"已有任何 `.agentdash` 提及即跳过"(W2 起的旧安装器
   维持原状,其余用户项目不受影响);
2. **钩子生效时机**:本会话早于配置创建,工具调用未产生 zcode 事件——与
   官方文档"会话启动加载"一致,`enabled:true` 与三事件已验证就位,**重开
   会话即自动接管本仓事件登记**(该验证标记为重开后事项,不预记结果);
3. svg 测试断言再次耦合台账任务数(3 任务的 W8 台账 < 5)——改为波次无关
   (背景 + ≥1 节点)。

## 帧证据

- `w8-close-panel.txt`:3/3 done,双 gate passed(claude 归属,本会话手工
  登记;zcode 钩子重开后自动接管)。
